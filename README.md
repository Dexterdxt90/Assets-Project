🛠️ Tech StackBackend: Python 3.10+, FastAPI, Pydantic, Uvicorn, APSchedulerDatabase: SQLite3Integrations: Microsoft Graph API, MSAL (msal), RequestsFrontend: HTML5, Modern JavaScript (ES6+), CSS3Packaging & Desktop: pywebview, PyInstaller🚀 Getting StartedPrerequisitesPython: Version 3.10 or higherPowerShell: Version 5.1+ (Windows) or Terminal (Linux/macOS)Azure AD / Microsoft Entra ID Application: App registration with Graph API permissions (for Intune synchronization)InstallationClone the repository:PowerShellgit clone [https://github.com/Dexterdxt90/Assets-Project.git](https://github.com/Dexterdxt90/Assets-Project.git)
cd Assets-Project
Create and activate a virtual environment:Windows (PowerShell):PowerShellpython -m venv venv
.\venv\Scripts\Activate.ps1
Linux / macOS:Bashpython3 -m venv venv
source venv/bin/activate
Install dependencies:PowerShellpip install fastapi uvicorn msal requests apscheduler pydantic pywebview pyinstaller
Run the backend server locally:PowerShelluvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
Access the application:Frontend UI: Open frontend/index.html in your browser or navigate to http://localhost:8000Interactive API Docs (Swagger): http://localhost:8000/docs🔐 Microsoft Intune IntegrationTo enable automatic device fetching from Intune:Go to Microsoft Entra ID Portal > App Registrations > New Registration.Name your app (e.g., Adcos-Intune-Sync) and register.Note down the Application (client) ID and Directory (tenant) ID.Go to Certificates & secrets > Create a new Client Secret (copy the value immediately).Go to API permissions > Add a permission > Microsoft Graph > Application permissions:Add DeviceManagementManagedDevices.Read.AllClick Grant admin consent for [Your Organization].Configure the credentials in backend/main.py or as environment variables:TENANT_IDCLIENT_IDCLIENT_SECRET💻 Deployment Options1. Standalone Desktop App (Executable)To bundle the application into a single executable folder for end-users:PowerShellpyinstaller --noconfirm --onedir --windowed --add-data "frontend/index.html;frontend" --add-data "frontend/app.js;frontend" desktop.py
The output executable will be created in the dist/desktop/ directory.2. On-Premises Windows Service (NSSM)To run the application continuously on a local Windows Server without requiring an open terminal session:Download NSSM (Non-Sucking Service Manager).Run PowerShell as Administrator and install the service:PowerShellnssm install AdcosInventario
In the GUI dialog:Path: Path to Python inside your virtual environment (C:\\path\\to\\venv\\Scripts\\python.exe)Startup directory: Project root folder (C:\\path\\to\\estoque-dispositivos)Arguments: -m uvicorn backend.main:app --host 0.0.0.0 --port 8000Start the service:PowerShellnet start AdcosInventario
3. Docker ComposeFor containerized server environments:Create a docker-compose.yml in the root folder and run:Bashdocker compose up -d --build
📖 API DocumentationFastAPI automatically generates interactive Swagger documentation:Swagger UI: http://localhost:8000/docsReDoc: http://localhost:8000/redocKey EndpointsMethodEndpointDescriptionGET/api/ativosList all assets with filtering and paginationPOST/api/ativosRegister a new asset manuallyPUT/api/ativos/{id}Update existing asset detailsDELETE/api/ativos/{id}Delete an asset recordPOST/api/intune/syncTrigger manual synchronization with Microsoft IntuneGET/api/metricsRetrieve overall inventory statistics and summary counts📄 LicenseDistributed under the MIT License. See LICENSE for more information."""with open("README.md", "w", encoding="utf-8") as f:f.write(readme_content)print("README.md successfully created.")
```text?code_stdout&code_event_index=1
README.md successfully created.

Your Markdown file is ready README  MD AbertoO arquivo README.md completo e estruturado foi gerado e está pronto para ser adicionado ao seu repositório no GitHub!📌 O que foi incluído no README:Badges e Apresentação: Indicadores visuais para Python, FastAPI, SQLite, Microsoft Graph API e licença MIT.Índice (Table of Contents): Navegação rápida por todas as seções do repositório.Funcionalidades Principais:Sincronização automatizada com o Microsoft Intune via Graph API.Lógica de Upsert mantendo metadados locais preservados.Agendamento em segundo plano (APScheduler) + gatilho manual via endpoint REST.Controle de ciclo de vida e status dos ativos.Estrutura de Pastas do Projeto: Mapeamento do backend/, frontend/ e desktop.py.Guia Passo a Passo de Instalação e Execução: Comandos para PowerShell e Linux/macOS.Passo a Passo de Configuração do Azure AD / Entra ID: Permissões necessárias (DeviceManagementManagedDevices.Read.All) e configuração de chaves.Opções de Deploy (Implantação):Aplicativo Executável Desktop: Instruções para compilar com PyInstaller e pywebview.Serviço Windows On-Premises: Passos para rodar 24/7 com NSSM.Docker Compose: Para ambientes baseados em contêineres.Documentação da API: Tabela dos principais endpoints REST (/api/ativos, /api/intune/sync, /api/metrics).
